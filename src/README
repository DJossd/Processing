//http://adrianmoya.com/2012/07/usando-git-de-manera-local/#sthash.PFdF8HAu.dpbs

// git config --global user.name "Firstname Lastname"
// git config --global user.email "your_email@youremail.com"

// git config --global color.ui true
// git init

//crear rama nueva
// git branch nombre_rama

//informacion detallada de las ramas
// git show-branch

//cambiar de rama de trabajo
// git checkout nombre_rama

//crear rama y cambiar a ella en un solo comando
// git checkout -b otra_rama

//borrar rama en local
// git branch -d rama_a_borrar

//borrado forzado independiente si se unieron ramas 
// git branch -D rama_a_borrar

…or crear repositorio online
echo "# Processing" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/DJossd/Processing.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/DJossd/Processing.git
git branch -M main
git push -u origin main